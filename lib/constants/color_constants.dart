import 'package:flutter/material.dart';

const Color electricViolet = Color(0xFF7349FE); //Homepage Button Color
const Color wildSand = Color(0xFFF6F6F6); //background color

const Color radicalRed = Color(0xFFFE3577); // Taskpage button color
const Color aquaMarine = Color(0xFF79FCD6); //
const Color valhalla = Color(0xFf211551); // TITLE AND
const Color waterloo = Color(0xFf86829D); // TEXT COLOR DESCRIPTION
const Color black = Colors.black; // black
const Color white = Colors.white; // white task's background color

const Gradient kPrimaryGradientColor = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [Color(0xFF7349FE), Color(0xFF160D35)],
);
